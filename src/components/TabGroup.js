import React from 'react';

const TabGroup = ({children}) => (
	<div id="tabgroup" style={{backgroundColor: 'red'}}>
        <h1>Tab Group</h1>
        {children}
    </div>
)

export default TabGroup 